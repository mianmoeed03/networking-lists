# networking-lists
computer networking and types(LAN,WAN,MAN,PAN),
IP address and its types(public or private),
Router,
Port numbers(to find specific server),
DNS(convert domain name into IP addresses),
HTTP and HTTPs(much safer),
Routing modes(user excessive,priveledge excessive,Global configuration)
Firewall(acts as a barrier to control traffic),
TCP(used in internet),
OSI model(used for understanding)
